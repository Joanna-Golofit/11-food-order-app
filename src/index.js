import ReactDOM from 'react-dom/client';

import './index.css';
import App from './App';
// import Modal from './components/UI/Modal';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);

// const overlays = ReactDOM.createRoot(document.getElementById('overlays'));
// overlays.render(<Modal />);
